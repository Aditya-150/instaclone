import React from "react";
import { View, Text, SafeAreaView } from "react-native";

const NewPostScreen = () => {
  return (
    <SafeAreaView>
      <AddNewPost/>
    </SafeAreaView>
  );
};

export default NewPostScreen;
